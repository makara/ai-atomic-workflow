{
  "name": "graph-workflow",
  "version": "0.0.0",
  "description": "Graph-workflow domain — built-in skills for graph lifecycle orchestration",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vitest": "^4.1.10",
    "yaml": "2.9.0"
  }
}
