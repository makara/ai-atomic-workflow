# DDD Domains Standard and Index

> This file = the repo's fine-grained DDD domain standard + index single source; `docs/domains/` directory tree forbidden. Granularity: one domain per skill / per graph / per feature point. References: clean-ddd-hexagonal (bounded context / subdomain classification / dependency rules) + codebase-design (seam / depth / locality / leverage). Division of labor: this file governs "who owns what"; glossary governs term disambiguation. No duplication.

## Design Requirements

Binding constraints on domain design (split, boundary, kind, naming) - standing equal to `.graph-scheduler/constraints.json`; every maintenance pass reads and complies. User-proposed, grill-confirmed via the estate-maintain user-request pass; recorded as bullets, one simple sentence each. Requirements never create domains and never substitute asset-derived provenance.

- Domain index covers only packages/-owned domains (skill/graph/engine-feature) and necessary supporting domains (e.g. test, release).
- No Doc-Family domain kind - documentation standards are carried by skills and other documents.
- No Virtual domain kind - virtual specs deduplicate into their corresponding entity domains.
- Deleted-domain spec content: non-packages content deleted outright; packages-related content merges into the corresponding entity domain spec.
- CONTEXT.md, domains.md, openspec/specs, and docs/adr content pure English — all prose (titles, headings, paragraphs, tables, list items, link text); code/identifiers/paths untouched.
- Existing non-English content in domains.md, openspec/specs, and docs/adr translated to English.
- Merged source domains are deleted entirely with no residual references in estate documents; ADR decision records stay exempt as historical evidence.
- Merge the four new engine-feature domains (context-channel-dedup, context-usage-check, compress-contract, context-hints) into two: context-channel-dedup folds into context-channels; the other three merge into the new domain context-usage-contract.
- RETIRED domains are grouped in a dedicated index section, with evidence per row, never inlined in active tables.
- No file or directory named after a retired domain remains in the spec estate or the live decision-record estate (archived records excluded).
- Split the signal distribution concept into three glossary entries and three engine-feature domains: signal-distribution (mechanism, seam map, fidelity ladder, native tier, zero denial), token-lifecycle (three-face token model, P0–P3 prompt classes, resident prompts), display-minimalism (three display laws, display tiering, per-call echo).
- Register token-lifecycle and display-minimalism as active core engine-feature domains with spec dirs partitioned from the signal-distribution spec; both depend on signal-distribution.
- Repoint packages/ cross-references from the signal-distribution entry to the new entries by concept; deployment copies never edited.
- openspec/specs/ top-level dirs SHALL match docs/domains.md domain IDs one-to-one; nested sub-specs are domain-internal documents.
- specs-sync SHALL check spec content for stale references to retired contracts.

## Split Standard

### Granularity Rules

|Rule|Content|
|-|-|
|R1|Each skill = one domain (`packages/graph-workflow/skills/` 17)|
|R2|Each graph = one domain (`packages/graph-scheduler/graphs/` 12)|
|R3|Each engine feature point = one domain (graph-scheduler module family, e.g. fsm / run-record / approval / routing / constraints / channels / prologue)|
|R4|Domain index scope per Design Requirements (packages/-owned + necessary supporting domains)|

### Naming Conventions

- Domain ID = asset name, strictly matched, kebab-case, **prefix kept** (`atom-pilot` → domain `atom-pilot`; `arch-review-loop` → domain `arch-review-loop`).

### kind Classification (per Current State)

|kind|Meaning|Domain count|
|-|-|-|
|`skill`|Built-in skill domain|18|
|`graph`|Built-in graph domain|12|
|`engine-feature`|Engine feature-point domain|31|

### Status Tags

`active` (current) / `retired` (superseded or removed capability — keeps a row marked retired for trace) / `deprecated` (legacy — takes no row, annotation only). Legacy skills under root `skills/` (9 SKILL.md — removed from disk 2026-08-04) noted as a whole, not listed as index rows.

## Dependency Rules

- Inter-domain dependencies declared **one-way**, cycles forbidden.
- Layer alignment (clean-ddd-hexagonal dependency direction): `engine-feature` (infrastructure) → `graph` (orchestration) → `skill` (primitive) → pure primitive layer; `skill` domains do not depend on `engine-feature` implementation details.
- Entry skills depend on their dispatch/spec primitives (e.g. `atom-pilot` → `atom-phase-handler`; `atom-phase-handler` → `atom-kernel`) — reference relations are dependencies.
- Declare upstream/downstream when adding a domain; violation = documentation defect.
- **Upstream declaration rule**: the "Dependencies" column of each detail table declares downstream (whom this domain depends on); upstream (who depends on this domain) = mechanical reverse lookup of the Dependencies columns — scan the Dependencies column of every detail table; every row containing the target domain ID is its upstream. Dependency direction is queryable: downstream via the Dependencies column, upstream via reverse lookup, closed in both directions.

## Overview Table

|ID|kind|Section|Status|Subdomain|
|-|-|-|-|-|
|atom-kernel|skill|Skill domain|active|core|
|atom-pilot|skill|Skill domain|active|core|
|atom-phase-handler|skill|Skill domain|active|core|
|atom-scope-interview|skill|Skill domain|active|core|
|entry-skill-contract|skill|Skill domain|active|core|
|atom-skill-spec|skill|Skill domain|active|core|
|atom-graph-spec|skill|Skill domain|active|core|
|atom-graph-design|skill|Skill domain|active|core|
|atom-graph-writer|skill|Skill domain|active|core|
|atom-doc-lifecycle|skill|Skill domain|active|supporting|
|atom-doc-maintain|skill|Skill domain|active|supporting|
|atom-domain-spec|skill|Skill domain|active|supporting|
|atom-spec-maintain|skill|Skill domain|active|supporting|
|atom-adr-maintain|skill|Skill domain|active|supporting|
|setup-atomic-workflow|skill|Skill domain|active|supporting|
|release-prep-analyze|skill|Skill domain|active|core|
|release-prep-apply|skill|Skill domain|active|core|
|first-principles|skill|Skill domain|active|core|
|e2e-minimal|graph|Graph domain|active|core|
|arch-review|graph|Graph domain|active|core|
|arch-review-loop|graph|Graph domain|active|core|
|adopt-with-docs|graph|Graph domain|active|core|
|openspec-apply|graph|Graph domain|active|core|
|openspec-engineer|graph|Graph domain|active|core|
|spec-implement|graph|Graph domain|active|core|
|graph-generate|graph|Graph domain|active|core|
|estate-maintain|graph|Graph domain|active|core|
|release-prep|graph|Graph domain|active|core|
|graph-maintain|graph|Graph domain|active|core|
|first-principles-dev|graph|Graph domain|active|core|
|scheduler-runtime|engine-feature|Engine-feature domain|active|core|
|fsm|engine-feature|Engine-feature domain|active|core|
|graph-definition|engine-feature|Engine-feature domain|active|core|
|graph-flow|engine-feature|Engine-feature domain — flow transition-table syntax sugar (ADR 0240): top-level `flow` field (mermaid-subset edges, unlabeled = sequence default, self-edge = bounded loop) compiled into node × condition → target; condition-matched advance + graph-internal jump channel (backward-only ∪ `__handoff`); loops = flow self-edges (frontend zero loop perception)|active|core|
|graph-flow-layout|engine-feature|Engine-feature domain — canonical graph-file layout for builtin graphs (ADR 0241): top-level key order `flow` before `inventory` / `constraints` after `inventory`; builtin graphs SHALL declare full flow blocks|active|core|
|graph-registry|engine-feature|Engine-feature domain|active|core|
|graph-generate-identity|engine-feature|Engine-feature domain|active|core|
|phase-handler|engine-feature|Engine-feature domain|active|core|
|approval|engine-feature|Engine-feature domain|active|core|
|auto-decision-rationale|engine-feature|Engine-feature domain|active|core|
|context-channels|engine-feature|Engine-feature domain|active|core|
|channels-context-model|engine-feature|Engine-feature domain|active|core|
|run-record|engine-feature|Engine-feature domain|active|core|
|graph-mcp-api|engine-feature|Engine-feature domain|active|core|
|graph-perception-list|engine-feature|Engine-feature domain|active|core|
|constraint-layering|engine-feature|Engine-feature domain|active|core|
|branch-route-enforcement|engine-feature|Engine-feature domain|active|core|
|todo-lifecycle|engine-feature|Engine-feature domain — todo node-boundary lifecycle (per-node scratchpads, handler boundary clears, todo() spelling, graph todo-ignorant; usage agent-discretionary within boundary — projection retired per ADR 0122)|active|core|
|tool-usage-contract|engine-feature|Engine-feature domain — tool usage contract (deterministic triggers, class-based Tool usage check + violation markers, pilot stats)|active|core|
|serena-single-engine|engine-feature|Engine-feature domain — serena single-engine execution (ADR 0123): core classes (locate/read/write/verify/run) chain length exactly 1, sole tool serena, zero fallback, unavailable → loud failure; intra-serena tiering (symbol LSP / FS all languages); run via platform shell (bash, rtk prefix) per the run chain (ADR 0125); register_edit unconditional while jcodemunch in use (scenario write block carries the reminder, ADR 0200)|active|core|
|scenario-tool-hints|engine-feature|Engine-feature domain — scenario-keyed unified hint layer (ADR 0200 + change hint-tool-context + hints-structure-simplify): closed scenario registry {find/read/write/verify/run} → hint blocks integrating the promoted formal tool set (no "HLT" wording); post-hoc primary channel with compliant-invocation suppression (a promoted tool used correctly attaches nothing — consumer display fn inline tool-name-set judgment, PROMOTED_TOOL_MAP deleted); pre-execution interception removed (machinery deleted, ADR 0214 — stays default off); decision-time static injection (resident enumeration derived from the block data source); static positive multi-group blocks (no template interpolation, consumer map data-driven removed)|active|core|
|graph-fidelity|engine-feature|Engine-feature domain — platform-seam signal discipline module (ADR 0151/0152 + R2 suspension ADR 0175 + module split ADR 0176 + hints-structure-simplify): echo-only chain (identity + progress) in pure data core (flat `src/` pcl/resident-data/hints — `src/core/` layer deleted; R1 chain SDK-owned since ADR 0195; reduction/metering/settlement machinery suspended — reference tree relocated to graph-fidelity-context); interface encapsulation layer (`src/interfaces/` ToolDeny + ToolHints only, type-only; SignalLifecycle + DisplayFeedback + effect Schema contracts moved/deleted, ADR 0195); platform hook registration consumed via the platform-hooks-sdk contract (bind(adapter, handlers) — SDK adapters own platform typing; ADR 0192), one parametric shape seam (`core/shapes.ts`); native distribution (`omp.extensions` manifest + `exports["./server"]` + marketplace catalog entry; no generated deploy copy, ADR 0153); observability = PCL mark only (appendEntry); subagent coverage platform-verified; supersedes signal-seams + context-fidelity|active|core|
|graph-fidelity-context|engine-feature|Engine-feature domain — R2 cost-economy context-management package (ADR 0186 rebuild — revokes the ADR 0175 suspension and supersedes the ADR 0176 reference-tree split): first-sight classification (signal-class dimension C1–C4 + four-dimension tags, ADR 0189), reduction (headroom MCP compress / native trim / discard), first-sight markers, session benefit ledger + settlement telemetry (ADR 0190/0191), publishable (private removed), zero-config auto-enable; loading = manifest single entry, docking = direct platform-hooks-sdk dependency (bind registry; module-local singleton; no manifest-second-entry, ADR 0192)|active|core|
|platform-hooks-sdk|engine-feature|Engine-feature domain — platform hooks unified SDK (ADR 0192): unified HookSet contract (sync + async dual-face API), adapter translation tables, bind(adapter, handlers) registry, standalone install/test/publish; consumed by graph-fidelity + graph-fidelity-context|active|core|
|execution-output|engine-feature|Engine-feature domain|active|core|
|prompt-assembly-probe|engine-feature|Engine-feature domain — probe test suite instrumenting the OMP prompt-assembly process and platform seams (npm `@oh-my-pi/pi-coding-agent@17.2.12` exact; zero reference-tree usage; E-group OMP face = native tier (context seam / supersedeReads / dropUseless); F-group position coordinates per seam map (no S4 — tool surface seam-governed); G-group live seam assertions + installed-plugin (`omp plugin list`) + distribution-manifest assertions; coordinates per signal-distribution seam map)|active|supporting|
|signal-distribution|engine-feature|Engine-feature domain — signal distribution standard (ADR 0146/0150): seam map (C1–C4 × seam × fidelity L0–L3), seam law, platform native tier, R1–R5 needs-basis|active|core|
|token-lifecycle|engine-feature|Engine-feature domain — token lifecycle governance: three-face model (prompt/context/feedback), P0–P3 prompt classification, resident prompts|active|core|
|display-minimalism|engine-feature|Engine-feature domain — display minimalism: three display laws, display tiering, per-call echo surfaces|active|core|
|questioning-primitives|engine-feature|Engine-feature domain — questioning primitives: scenario-separated approval/interview/grilling contracts, explicit interview() participation, grilling graph-dispatch encapsulation|active|core|
|task-templates|engine-feature|Engine-feature domain — builtin task-template directory (ADR 0232): compile-time content assets in `src/task-templates/` (handoff TS module — session contract, index registration surface + TASK_TEMPLATES list = {handoff}); new builtin template = new file + index export, zero compiler edits|active|core|

85 domains total (20 skill — 18 active + 2 retired — + 19 graph — 12 active + 7 retired — + 46 engine-feature — 31 active + 15 retired). Status: 61 active + 24 retired.

## Retired Domains (24)

Rows kept for trace per status-tag semantics (superseded or removed capability); evidence per row. Assets and spec dirs were deleted with retirement; residue checks stay zero every pass.

|Domain ID|kind|Retired via|Notes|
|-|-|-|-|
|hlt-heat-layering|engine-feature|specs-sync merged-source deletion + row retirement (round-7 E1 adjudication, change spec-estate-align, 2026-08-14)|RETIRED — heat-layering content merged into high-level-tool / serena-single-engine; spec dir removed by specs-sync with the merged-source deletion; row kept for trace|
|atom-atomic-step|skill|ADR 0117/0120|SUPERSEDED — Atomic Step Protocol chapter deleted, superseded by the High-Level Tool Registry (ADR 0119); standalone skill deleted; spec removed with the ASP chapter|
|atom-mcp-contract|skill|ADR 0120|SUPERSEDED — merged into atom-kernel (HLT Registry + tool schemas + schema-first protocol); standalone skill deleted; spec retired|
|context-delivery-fidelity|engine-feature|ADR 0115/0121|SUPERSEDED then retired — machinery stays deleted; channel file consumption follows the HLT read chain; spec deleted|
|structural-channel-materialization|engine-feature|ADR 0121|RETIRED — scheduler materialization (map headers/contextBytes) removed; HLT single standard — see tool-usage-contract, graph-mcp-api, atom-kernel; spec deleted|
|signal-seams|engine-feature|ADR 0151|RETIRED — merged into graph-fidelity (per-call discipline echo + observability); tree deleted, no backward compatibility; spec retired with the merge|
|omp-adapter|engine-feature|ADR 0146|RETIRED — OMP platform adapter prototype superseded by the signal-distribution layer (context-fidelity / run frame); tree deleted, no backward compatibility|
|opencode-hlt-policy|engine-feature|ADR 0146|RETIRED — plugin prototype superseded by the signal-distribution layer (context-fidelity / run frame); tree deleted, no backward compatibility; spec retired|
|context-fidelity|engine-feature|ADR 0151|RETIRED — absorbed into graph-fidelity (fidelity transform core; TTSR rule file deleted, ADR 0153 + 0154); tree deleted, no backward compatibility; spec retired with the merge|
|activation-prologue|engine-feature|ADR 0148|RETIRED — prologue node machinery (run-mode confirmation + constraints loading) removed; activation facts live at the invocation boundary (pilot loads the constraints compiled artifact; per-activation run mode deleted later, ADR 0215); asset prologue.ts deleted; spec retired|
|high-level-tool|engine-feature|ADR 0194 + change scenario-tool-hints (2026-08-16)|RETIRED — High-Level Tool Registry instruction layer (ADR 0119/0123) removed with the registry (HLT-REGISTRY.md deleted, ADR 0194); content superseded by scenario-tool-hints (scenario-keyed unified hints) + serena-single-engine; spec dir deleted in tree; row kept for trace|
|mutation-plane|engine-feature|ADR 0138 fold + change scenario-tool-hints (2026-08-16)|RETIRED — mutation + ground-truth plane behavior folded into high-level-tool (spec deleted, ADR 0138), now superseded by serena-single-engine write posture + write scenario block; row kept for trace|
|query-plane|engine-feature|ADR 0138 fold + change scenario-tool-hints (2026-08-16)|RETIRED — query plane behavior folded into high-level-tool (spec deleted, ADR 0138), now superseded by the find/read scenario blocks (jcodemunch query head + serena ground truth); row kept for trace|
|atomic-step-flows|engine-feature|ADR 0138 fold + change scenario-tool-hints (2026-08-16)|RETIRED — cross-plane flow contract folded into high-level-tool (spec deleted, ADR 0138), now superseded by the scenario hint blocks' usage posture; row kept for trace|
|context-usage-contract|engine-feature|ADR 0175 + change archive (graph-fidelity-r2-suspend)|RETIRED — R2 cost economy suspended pending redesign (ADR 0175); spec removed at change archive; skill reference sections removed with the registry (ADR 0194); row kept for trace|
|graph-cross-run-delegation|engine-feature|ADR 0233 + change graph-langgraph-subgraph-align (2026-08-19)|RETIRED — cross-run delegation removed with the composed execution surface (ADR 0233): delegation node synthesis + DELEGATION_TASK + sibling-run launch + report collection deleted; spec dir deleted at change archive; row kept for trace|
|subgraph-execution-mode|engine-feature|ADR 0233 + change graph-langgraph-subgraph-align (2026-08-19)|RETIRED — composed execution-mode hint + dispatch position removed (ADR 0233): `execution` field (subagent/cross-run) and NodeDetail `position`/`executionMode` deleted; spec dir deleted at change archive; row kept for trace|
|fp-loop-body|graph|ADR 0240 + change graph-flow-mermaid-refactor (2026-08-20)|RETIRED — loop-body subgraph for first-principles-dev (per-round re-entry); loop desubgraphified into the parent's flow self-edge (`fp-doc-update -->|remaining|scope-entry`, ADR 0240); registry entry removed, yaml file deleted|
|arch-loop-body|graph|ADR 0240 + change graph-flow-mermaid-refactor (2026-08-20)|RETIRED — loop-body subgraph for arch-review-loop (round re-entry); loop = parent flow self-edge (`round-report -->|remaining|scope-entry`, ADR 0240); yaml file + registry entry deleted|
|apply-review-body|graph|ADR 0240 + change graph-flow-mermaid-refactor (2026-08-20)|RETIRED — loop-body subgraph for openspec-apply (change-review rework); loop = parent flow self-edge (`change-review -->|fail|apply-change`, ADR 0240); yaml file + registry entry deleted|
|engineer-review-body|graph|ADR 0240 + change graph-flow-mermaid-refactor (2026-08-20)|RETIRED — loop-body subgraph for openspec-engineer (implement-review rework); loop = parent flow self-edge (`implement-review -->|fail|implement`, ADR 0240); registry entry removed, yaml file deleted|
|generate-review-body|graph|ADR 0240 + change graph-flow-mermaid-refactor (2026-08-20)|RETIRED — loop-body subgraph for graph-generate (review rework); loop = parent flow self-edge (`review -->|fail|implement`, ADR 0240); yaml file + registry entry deleted|
|maintain-review-body|graph|ADR 0240 + change graph-flow-mermaid-refactor (2026-08-20)|RETIRED — loop-body subgraph for graph-maintain (execute+review rework); loop = parent flow self-edge (`review -->|fail|execute`, ADR 0240); yaml file + registry entry deleted|
|release-prep-body|graph|ADR 0240 + change graph-flow-mermaid-refactor (2026-08-20)|RETIRED — loop-body subgraph for release-prep (review rework); loop = parent flow self-edge (`review -->|fail|apply`, ADR 0240); yaml file + registry entry deleted|

## Skill Domains (18 · active)

|Domain ID|Description|Asset|Aggregate specs|Dependencies|Subdomain|
|-|-|-|-|-|-|
|atom-kernel|Platform primitives + sole reference skill — task()/approval()/interview()/todo() + tool schemas (tool discipline delivered by graph-fidelity scenario-keyed hints; tool schemas for serena/jcodemunch/graph-scheduler; ADR 0194) + graph tool detection|`packages/graph-workflow/skills/atom-kernel/SKILL.md`|`openspec/specs/atom-kernel/spec.md`|— (platform layer)||core|
|atom-pilot|Graph lifecycle management — execute→advance loop|`packages/graph-workflow/skills/atom-pilot/SKILL.md`|`openspec/specs/atom-pilot/spec.md`|atom-phase-handler, atom-kernel||core|
|atom-phase-handler|Central dispatch — single main path (type-agnostic frontend)|`packages/graph-workflow/skills/atom-phase-handler/SKILL.md`|`openspec/specs/atom-phase-handler/spec.md`|atom-kernel||core|
|atom-scope-interview|Parameterized entry procedure — caller-declared contract (Topics / Output fields / Behavior flags), zero reverse references, delegates to interview() consensus (ADR 0126)|`packages/graph-workflow/skills/atom-scope-interview/SKILL.md`|`openspec/specs/atom-scope-interview/spec.md`|atom-kernel||core|
|entry-skill-contract|Entry-skill callee contract pattern (ADR 0126) — task text is the parameter channel; behavior = f(declared parameters), never caller identity; packages/-only ownership boundary|Shared assets: `packages/graph-workflow/skills/atom-scope-interview/SKILL.md`, `packages/graph-workflow/skills/atom-graph-spec/SKILL.md` (§Entry-Skill Contract Declarations)|`openspec/specs/entry-skill-contract/spec.md`|atom-kernel||core|
|atom-skill-spec|SKILL.md format reference|`packages/graph-workflow/skills/atom-skill-spec/SKILL.md`|`openspec/specs/atom-skill-spec/spec.md`|—||core|
|atom-graph-spec|.yaml format reference|`packages/graph-workflow/skills/atom-graph-spec/SKILL.md`|`openspec/specs/atom-graph-spec/spec.md`|—||core|
|atom-graph-design|Graph topology design entry skill — loads atom-graph-spec, designs phase list (dependsOn/when/channels); trigger: graph-generate spec phase|`packages/graph-workflow/skills/atom-graph-design/SKILL.md`|`openspec/specs/atom-graph-design/spec.md`|atom-graph-spec||core|
|atom-graph-writer|Graph YAML generation entry skill — loads atom-graph-spec, validates topology, writes `.yaml`; trigger: graph-generate implement phase|`packages/graph-workflow/skills/atom-graph-writer/SKILL.md`|`openspec/specs/atom-graph-writer/spec.md`|atom-graph-spec||core|
|atom-doc-lifecycle|End-of-workflow closure deep module — close() contract (reverse-validated archive + ADR decision-fold + index rebuild)|`packages/graph-workflow/skills/atom-doc-lifecycle/SKILL.md`|`openspec/specs/atom-doc-lifecycle/spec.md`|—||supporting|
|atom-doc-maintain|Doc estate maintenance deep module — maintain() contract (trigger classification/doc classification/maintenance rules/consistency gate) + Format Reference|`packages/graph-workflow/skills/atom-doc-maintain/SKILL.md`|`openspec/specs/atom-doc-maintain/spec.md`|atom-domain-spec||supporting|
|atom-domain-spec|docs/domains.md format reference — DDD split principles per clean-ddd-hexagonal (bounded-context judgment + core/supporting/generic subdomain classification) + domain-modeling ubiquitous language; 10-100 count bound + kind layering; reverse-analysis provenance (asset→domain, no asset no domain, no forward design); evolution four-step; head-position Design Requirements constraint block (constraints.json equal standing) + linkage rule (spec/ADR only inside domain lists)|`packages/graph-workflow/skills/atom-domain-spec/SKILL.md`|`openspec/specs/atom-domain-spec/spec.md`|—||supporting|
|atom-spec-maintain|openspec/specs estate maintenance contract — reverse-analysis triple diff (actual capabilities ↔ domains.md ↔ spec dirs) → minimal change (delta specs only) → openspec-sync-specs → archive; spec dirs ↔ domain IDs 1:1|`packages/graph-workflow/skills/atom-spec-maintain/SKILL.md`|`openspec/specs/atom-spec-maintain/spec.md`|atom-domain-spec, atom-doc-maintain||supporting|
|atom-adr-maintain|ADR estate alignment contract — status verification vs decision reality; stale chains folded via atom-doc-lifecycle fold machinery; index rebuild; archive hygiene; dead citations repointed|`packages/graph-workflow/skills/atom-adr-maintain/SKILL.md`|`openspec/specs/atom-adr-maintain/spec.md`|atom-doc-lifecycle, atom-doc-maintain||supporting|
|setup-atomic-workflow|Project graph config initialization|`packages/graph-workflow/skills/setup-atomic-workflow/SKILL.md`|`openspec/specs/setup-atomic-workflow/spec.md`|—||supporting|
|release-prep-analyze|Pre-release analysis — version proposal from git tag history (never package.json) + changelog inventory from one diff scan; deterministic + idempotent pre-tag, never executes git tag/commit/push|`packages/graph-workflow/skills/release-prep-analyze/SKILL.md`|`openspec/specs/release-prep/spec.md`|atom-kernel||core|
|release-prep-apply|Pre-release writes — version bump on release-line surfaces, CHANGELOG fold per spec, README list sync vs ground truth; overwrite-style, idempotent, per-domain verification|`packages/graph-workflow/skills/release-prep-apply/SKILL.md`|`openspec/specs/release-prep/spec.md`|atom-kernel||core|
|first-principles|Vendored first-principles thinking methodology — deletion-only trimmed copy of the upstream skill (ClawFu, MIT): assumption audit → law-vs-convention breakdown → atomic decomposition → rebuild from fundamentals; repo-owned asset so builtin graphs (arch-review, first-principles-dev) resolve the skill inside packages/|`packages/graph-workflow/skills/first-principles/SKILL.md`|`openspec/specs/first-principles/spec.md`|—||core|

- Domain spec: one per domain (59 top-level spec dirs on disk — first-principles + first-principles-dev + graph-perception-list spec dirs pending openspec archive sync, the sole spec-sync channel; graph-generate carries the merged maker-journey requirements (former workflow-scenarios aggregate); retired atom-mcp-contract, atom-atomic-step, context-delivery-fidelity, structural-channel-materialization, activation-prologue specs deleted with retirement; channel-tiers orphan retired with its three-tier content merged into channels-context-model; merged source domains of hlt-heat-layering deleted entirely (spec dirs removed by specs-sync — row retired 2026-08-14, spec-estate-align); readme-family orphan retired — doc-family content, no Doc-Family domain kind per Design Requirements, spec dir removed by specs-sync (2026-08-10); domains without an independent behavior contract regi…

## Graph Domains (12 · Status active)

|Domain ID|Description|Asset|Aggregate specs|Dependencies|Subdomain|
|-|-|-|-|-|-|
|e2e-minimal|Minimal main→confirmation loop demo|`packages/graph-scheduler/graphs/e2e-minimal.yaml`|`openspec/specs/e2e-minimal/spec.md`|—||core|
|arch-review|Requirement production, standalone (scope-entry → arch-review report → review-accept)|`packages/graph-scheduler/graphs/arch-review.yaml`; produces `docs/reports/`|`openspec/specs/arch-review/spec.md`|atom-scope-interview||core|
|arch-review-loop|Three-stage loop (requirement production → adopt → implement; single loop)|`packages/graph-scheduler/graphs/arch-review-loop.yaml`; produces `docs/reports/`|`openspec/specs/arch-review-loop/spec.md`|arch-review, adopt-with-docs, spec-implement, atom-scope-interview||core|
|adopt-with-docs|Requirement adoption (adopt stage) + spec production: standalone raw-idea entry; composed → report input, record appended as dated appendix (standalone: grilling-derived `docs/adopt/<date>-<slug>.md`, never asked); ADR decision always user-confirmed; adopted requirements materialize as the OpenSpec change|`packages/graph-scheduler/graphs/adopt-with-docs.yaml`; produces `docs/adopt/`|`openspec/specs/adopt-with-docs/spec.md`|atom-scope-interview||core|
|openspec-apply|Apply change → dual-axis review → bounded rework → plain archive (openspec-archive-change)|`packages/graph-scheduler/graphs/openspec-apply.yaml`|`openspec/specs/openspec-apply/spec.md`|openspec-archive-change||core|
|openspec-engineer|Detailed implementation (spec synthesis→tickets→tdd→dual review→lifecycle closure)|`packages/graph-scheduler/graphs/openspec-engineer.yaml`|`openspec/specs/openspec-engineer/spec.md`|atom-doc-lifecycle||core|
|spec-implement|Pure implementation of an existing change (extract→track gate→apply/engineer; no spec generation; tracks own post-archive closure)|`packages/graph-scheduler/graphs/spec-implement.yaml`|`openspec/specs/spec-implement/spec.md`|openspec-apply, openspec-engineer||core|
|graph-generate|Graph production — the maker journey (name states the operation): concrete 7-phase graph (entry → spec → spec-accept → implement → review → rework decision → accept); single kind (graph), single operation (create); entry confirms graph name + topology scope + save location (default `.graph-scheduler/graphs/`), no CONTEXT.md dependency; implement writes `.yaml` + registry entry (two-path bundle); no skill co-production|`packages/graph-scheduler/graphs/graph-generate.yaml`; produces `.graph-scheduler/graphs/`|`openspec/specs/graph-generate/spec.md` (maker-journey requirements — merged from workflow-scenarios)|atom-scope-interview, atom-graph-spec||core|
|estate-maintain|Estate maintenance graph — entry (trigger classification: domain-change/skill-change/proactive + workstream selection) → domains-index (atom-doc-maintain) / specs-sync (atom-spec-maintain) / adr-align (atom-adr-maintain) → review (consistency gate + reverse-validation + read-only deployment-mirror check) → accept|`packages/graph-scheduler/graphs/estate-maintain.yaml`|`openspec/specs/estate-maintain/spec.md`|atom-scope-interview, atom-doc-maintain, atom-spec-maintain, atom-adr-maintain, atom-domain-spec, atom-doc-lifecycle||core|
|release-prep|Pre-release preparation — propose (release-prep-analyze: version from git tag history, deterministic + idempotent pre-tag, never executes git tag/commit/push) → plan-grill (grilling confirmation of every planned operation — interview, never auto-gated) → apply (release-prep-apply: version bump on release-line surfaces + CHANGELOG [Unreleased] fold per spec + README list sync vs ground truth, overwrite-style + verified) → release-review (approval; continue completes the run — final report prints tag/commit commands, user executes manually; jump re-runs a phase)|`packages/graph-scheduler/graphs/release-prep.yaml`|`openspec/specs/release-prep/spec.md`|atom-kernel, release-prep-analyze, release-prep-apply||core|
|graph-maintain|Graph file maintenance (maintain operation) — the maintenance flow: entry (atom-scope-interview — target graph via graph_assets + maintenance intent) → audit (atom-graph-writer maintain mode: inventory compliance + content-vs-inventory) → propose (per-finding fix proposals) → approval (mandatory user gate) → execute (two-path bundle apply + load-probe) → review → rework decision → accept. Mirrors the graph-generate maker journey; pairs with the problem-surfacing channel (graph_start problems / graph_init full pass / graph_assets query)|`packages/graph-scheduler/graphs/graph-maintain.yaml`|`openspec/specs/graph-maintain/spec.md`|atom-scope-interview, atom-graph-writer||core|
|first-principles-dev|First-principles-prerequisite development flow — requirement (arch-review: scope + requirement/diff input → report → accept) → adopt (adopt-with-docs: confirms the report, appends dated appendix, produces the OpenSpec change) → implement (spec-implement: change → track machinery → archive) → fp-doc-update (folds requirement/diff + reasoning into docs/first-principles/development-flow.md per the fp README update-maintenance contract) → loop-gate (explicit unconditional re-entry to requirement/scope-entry — legal round-continue pattern; termination at the direct-end options)|`packages/graph-scheduler/graphs/first-principles-dev.yaml`; produces `docs/reports/`|`openspec/specs/first-principles-dev/spec.md`|arch-review, adopt-with-docs, spec-implement, atom-scope-interview||core|

## Engine-Feature Domains (30 · active)

|Domain ID|Description|Asset|Aggregate specs|Dependencies|Subdomain|
|-|-|-|-|-|-|
|scheduler-runtime|Runtime assembly (handler/FSM/persistence wiring)|`packages/graph-scheduler/src/scheduler-runtime.ts`|`openspec/specs/scheduler-runtime/spec.md`|fsm, graph-definition, graph-registry, phase-handler, run-record, graph-mcp-api, activation-prologue, constraint-layering||core|
|fsm|Pure-function state machine core (START/COMPLETE/JUMP/FORCE_END)|`packages/graph-scheduler/src/fsm/transition.ts`, `packages/graph-scheduler/src/fsm/events.ts`, `packages/graph-scheduler/src/fsm/effects.ts`, `packages/graph-scheduler/src/fsm/state-machine.ts`|`openspec/specs/fsm/spec.md`|— (pure functions, zero dependencies)||core|
|graph-definition|Graph definition loading/validation/flattening/topology/contract checks/routing semantics + data shapes (zod schemas + shared types)|`packages/graph-scheduler/src/graph-definition.ts`, `packages/graph-scheduler/src/flow-flatten.ts`, `packages/graph-scheduler/src/topology.ts`, `packages/graph-scheduler/src/types.ts`, `packages/graph-scheduler/src/schemas/phase.ts`, `packages/graph-scheduler/src/schemas/node-state.ts`, `packages/graph-scheduler/src/schemas/config.ts`, `packages/graph-scheduler/src/schemas/index.ts`, `packages/graph-scheduler/src/schemas/registry-entry.ts`, `packages/graph-scheduler/src/schemas/workflow.ts`, `packages/graph-scheduler/src/schemas/effect-wrapper.ts`|`openspec/specs/graph-definition/spec.md`|graph-registry, context-channels||core|
|graph-flow|Flow transition-table syntax sugar (ADR 0240) — top-level `flow` field: mermaid-subset transition edges (`A -->|condition|B`, unlabeled = sequence default, self-edge = bounded loop) parsed by a deterministic subset grammar and compiled into the per-node transition table (node × condition → target); condition-matched advance (mechanical string equality; no match fails loudly — missed-condition guard); graph-internal jump channel (target ⊆ topological ancestors ∪ `__handoff`, forward rejected loudly, retryCount++); loops = flow self-edges (inline bounded loops — frontend zero loop perception; loop template retired, TASK_TEMPLATES = {handoff, startup, router}); consumed by fsm (condition-match transitions) + graph-mcp-api (advance condition/jump params) + graph-maintain (flow audit axis — inventory-同轨 ownership)|`packages/graph-scheduler/src/flow.ts` (subset grammar parse), `packages/graph-scheduler/src/compile.ts` (transition-table compile + jump ancestor closure), `packages/graph-scheduler/src/adapter.ts` (condition lookup / jump guard)|`openspec/specs/graph-flow/spec.md`|graph-definition (top-level `flow` field in WorkflowSchema)|core|
|graph-flow-layout|Canonical graph-file layout for builtin graphs (ADR 0241) — top-level key order (`flow` before `inventory`, `constraints` after `inventory`); builtin graphs SHALL declare a full `flow` block (every declared phase covered by an edge; synthesized `__handoff` excluded from coverage); the WorkflowSchema field declaration order mirrors the canonical layout|`openspec/specs/graph-flow-layout/spec.md`|graph-flow||core|
|graph-registry|Multi-registry merging (built-in + project)|`packages/graph-scheduler/src/registry-loader.ts`|`openspec/specs/graph-registry/spec.md`|—||core|
|graph-generate-identity|Maker-graph identity — graph name states the operation, top-level `description`, registry project-first precedence with `resolvedFrom`, load-probe validation, runId-scoped outputs|`packages/graph-scheduler/src/registry-loader.ts` (project-first shadowing), `packages/graph-scheduler/src/api/graph-loader.ts` (resolvedFrom/resolvedPath/description), `packages/graph-scheduler/src/api/crud.ts` (graph_start identity fields), `packages/graph-scheduler/src/scheduler-runtime.ts` (identity banner types)|`openspec/specs/graph-generate-identity/spec.md`|graph-registry, graph-mcp-api||core|
|phase-handler|Single main handler + decision persistence|`packages/graph-scheduler/src/phase-handler/types.ts`, `packages/graph-scheduler/src/phase-handler/main-handler.ts`, `packages/graph-scheduler/src/phase-handler/index.ts`, `packages/graph-scheduler/src/phase-handler/errors.ts`|`openspec/specs/phase-handler/spec.md`|context-channels||core|
|approval|approval() decision primitive — single-form decision card, always presented, never auto-executed (absorbs question(), ADR 0133); run-mode mode dispatch removed (ADR 0215); interview() turns carry confirmations as cards without recommendation; implementation in atom-kernel (§approval() contract), rendered on main confirmation nodes|`openspec/specs/approval/spec.md`|atom-kernel||core|
|auto-decision-rationale|Auto-approval decision rationale — mechanism removed: the Run Mode auto path is deleted (ADR 0215); `rationale` survives only as the optional recommendation basis on the single-form approval() card (manual choices omit it)|`packages/graph-workflow/skills/atom-kernel/APPROVAL-CARDS.md` (§IApprovalDecision Shape)|`openspec/specs/auto-decision-rationale/spec.md`|atom-kernel||core|
|context-channels|Context contract parsing + channel resolution + convention-layer channel declaration guard (ADR 0145 fold) — convention-file entries (`DEFAULT_CONVENTIONS` members) in phase `channels:` / graph `context:` warn at validation and are skipped at dispatch (zero delivery; implicit coverage unchanged); channels carry node-specific streams only|`packages/graph-scheduler/src/context/contracts.ts`, `packages/graph-scheduler/src/context/resolve-channels.ts`|`openspec/specs/context-channels/spec.md`|—||core|
|channels-context-model|Two-scope context model — global channel (`context:`, config default layer) + node channels (output streams, read edges, promotion); uniform phase channels; single judgment-domain formula|Shared assets — behavior spec of the channels feature point; implementation files owned by graph-definition / graph-mcp-api / constraint-layering (schemas, flow-flatten, snapshot, config.json)|`openspec/specs/channels-context-model/spec.md`|context-channels, graph-definition, phase-handler||core|
|run-record|Run record persistence (libsql) + snapshot reconstruction|`packages/graph-scheduler/src/lib/db/repository.ts`, `packages/graph-scheduler/src/lib/db/schema.ts`, `packages/graph-scheduler/src/lib/db/migration.ts`, `packages/graph-scheduler/src/lib/db/helpers.ts`, `packages/graph-scheduler/src/lib/db/checkpoint-saver.ts`, `packages/graph-scheduler/src/adapter.ts` (snapshot reconstruction — single builder)|`openspec/specs/run-record/spec.md`|fsm||core|
|graph-mcp-api|9 MCP tools (CRUD/query/maintenance) + DTO + server process support|`packages/graph-scheduler/src/api/crud.ts`, `packages/graph-scheduler/src/api/snapshot.ts`, `packages/graph-scheduler/src/api/query.ts`, `packages/graph-scheduler/src/api/maintenance.ts`, `packages/graph-scheduler/src/api/graph-loader.ts`, `packages/graph-scheduler/src/runtime-start.ts`, `packages/graph-scheduler/src/debug.ts`, `packages/graph-scheduler/src/filesystem.ts`|`openspec/specs/graph-mcp-api/spec.md`|run-record, graph-definition, fsm||core|
|graph-perception-list|System-level graph info list (ADR 0235) — frontend perception of graphs mirroring skills perception: five-field `graph_assets` payload `{id, description, run_conditions, source, problems}` (run_conditions projects interaction + constraints presence from the graph definition — never a new fact source; source merges registered/resolvedFrom → builtin\|project\|fallback); registry = pure `{name, path}` index (description single-sourced at the graph definition top-level; tags axis deleted — user ruling); atom-pilot activation resident perception block (id + description per graph, mirroring the skills `<skills>` block; detail on demand; failure degrades); lifecycle produce (single-source projection, no in-memory cache — ADR 0188) / load (activation query + on-demand) / use (selection, run-condition awareness, problem surfacing → graph-maintain)|Shared assets — behavior spec of the perception-list feature point; implementation in graph-mcp-api (graph_assets payload — src/api/query.ts), graph-definition (top-level description single source + drift check), graph-registry (pure-index entries), atom-pilot (resident block)|`openspec/specs/graph-perception-list/spec.md`|graph-mcp-api, graph-definition, graph-registry, atom-pilot||core|
|constraint-layering|Three-layer constraint system (global/instructions/standards) + injection + validation|`.graph-scheduler/constraints.md`, `.graph-scheduler/config.json`|`openspec/specs/constraint-layering/spec.md`|—||core|
|branch-route-enforcement|Branch-route activation engine enforcement — a branch-route approval decision must not rely on caller re-transmission; a missed branchTo fails loudly (InvalidStateTransitionError), never drains silently|`packages/graph-scheduler/src/fsm/transition.ts` (branch-route enforcement)|`openspec/specs/branch-route-enforcement/spec.md`|fsm||core|
|todo-lifecycle|Todo node-boundary lifecycle — platform todo lists are per-node execution scratchpads; handler clears at dispatch/completion (unconditional, all node types); todo() kernel spelling (clear-only, no-todo platform no-op); graph stays todo-ignorant; usage agent-discretionary within the boundary (projection contract retired per ADR 0122)|Shared assets — behavior spec of the todo-boundary feature point; implementation in atom-phase-handler (§Todo Lifecycle) + atom-kernel (todo() spelling)|`openspec/specs/todo-lifecycle/spec.md`|atom-phase-handler, atom-kernel||core|
|tool-usage-contract|Tool usage contract — deterministic triggers (register_edit while jcodemunch in use; scenario-keyed hint registry per ADR 0200); class-based Tool usage check (per declared class, auto violation markers, missing block = all-class violation; declared classes resolve against registry keys, `n/a: no scenario coverage` otherwise); pilot tools stats|Shared assets — behavior spec of the tool-usage feature point; implementation in atom-kernel (§Tool Discipline — scenario registry) + atom-phase-handler (§Tool Usage Check — class-based) + atom-pilot (Tools stats)|`openspec/specs/tool-usage-contract/spec.md`|atom-kernel, atom-phase-handler, atom-pilot||core|
|scenario-tool-hints|Scenario-keyed unified hint layer (ADR 0200 + change hint-tool-context + hints-structure-simplify) — closed scenario registry {find/read/write/verify/run} → one hint block per scenario integrating the promoted formal tool set (tools + usage posture + verification obligations), no "HLT" wording; post-hoc tool_result attachment is the primary channel (append-only, fail-open, dual-face) with compliant-invocation suppression — classify returns {scenario?, compliant} (SDK native rules + rtk-prefixed bash = compliant run); the consumer display fn judges compliance inline via its tool-name sets (PROMOTED_TOOL_MAP deleted, hints-structure-simplify — blocks are static positive copy, no {usedTool}/{promoted} interpolation, no consumer classification data rides the SDK); a compliant invocation attaches nothing and emits no feedback; pre-execution interception removed (machinery deleted, ADR 0214 — no validation surface, stays default off); decision-time static injection (resident five-scenario enumeration DERIVED from the block data source, no parallel wording)|Shared assets — behavior spec of the scenario-hints feature point; implementation in graph-fidelity (texts template blocks + resident derivation + PROMOTED_TOOL_MAP) + platform-hooks-sdk core (scenarios/classify — compliance verdict + parameterized render); supersedes the retired high-level-tool registry instruction layer|`openspec/specs/scenario-tool-hints/spec.md`|graph-fidelity, platform-hooks-sdk, tool-usage-contract||core|
|serena-single-engine|Serena single-engine execution (ADR 0123) — core-class contract: locate/read/write/verify/run chains length exactly 1, sole tool serena, zero fallback (unavailable → loud failure); intra-serena tiering (symbol LSP / FS all languages) closes the coverage gap inside one dependency; run via platform shell (`bash`, rtk prefix) per the run chain (ADR 0125); register_edit unconditional while jcodemunch in use — the write scenario block carries the reminder (ADR 0200)|Shared assets — behavior spec of the serena-single-engine feature point; implementation in atom-kernel (§Tool Schemas) + graph-fidelity discipline (write scenario block) + .graph-scheduler/constraints.md (rule 8)|`openspec/specs/serena-single-engine/spec.md`|tool-usage-contract, atom-kernel, scenario-tool-hints||core|
|graph-fidelity|Platform-seam signal discipline (ADR 0151/0152 + module split ADR 0176 + hints-structure-simplify) — base module + interface layer: flat `src/` consumer-side content only (pcl/resident-data/hints — `src/core/` layer deleted, R1 chain modules moved to the SDK, ADR 0195) + `src/interfaces/` (ToolDeny type-only; SignalLifecycle + DisplayFeedback + effect Schema contracts removed, ADR 0195) + platform hook registration consumed via the platform-hooks-sdk contract (`bind(adapter, handlers)` — SDK adapters own the real platform typing, ADR 0192); distribution = `omp.extensions` manifest + `exports["./server"]` + marketplace catalog entry (registration surface; no generated deploy copy, ADR 0153); no rules/ asset (TTSR rule file deleted, ADR 0154)|`packages/graph-fidelity/` (src/ core + interfaces + adapters + diagnostics, test/)|`openspec/specs/graph-fidelity/spec.md`|signal-distribution|core|ro platform imports) + `src/interfaces/` (SignalLifecycle assembly/injection/echo/restore + DisplayFeedback audit; effect Schema contracts; `exports["./interfaces"]` type-only) + platform hook registration consumed via the platform-hooks-sdk contract (`bind(adapter, handlers)` — SDK adapters own the real platform typing, ADR 0192); distribution = `omp.extensions` manifest + `exports["./server"]` + marketplace catalog entry (registration surface; no generated deploy copy, ADR 0153); no rules/ asset (TTSR rule file deleted, ADR 0154); observability = PCL mark only (appendEntry — opencode absence declared); subagent coverage platform-verified; R2 cost economy suspended (ADR 0175) — reference tree relocated to graph-fidelity-context (ADR 0176); single frame lookup (role-order parameterization), single failure-diagnostics point (adapters/diagnostics.ts); supersedes signal-seams + context-fidelity|`packages/graph-fidelity/` (src/ core + interfaces + adapters + diagnostics, test/)|`openspec/specs/graph-fidelity/sp…|
|graph-fidelity-context|Active context-management package (ADR 0186 rebuild — revoked the ADR 0175 suspension; classification ADR 0189; display ADR 0190/0191): first-sight classify/reduce/mark/telemetry + session benefit ledger; publishable (private removed), zero-config auto-enable; loading = manifest single entry, docking = direct platform-hooks-sdk dependency (bind registry; module-local singleton; no manifest-second-entry, ADR 0192)|`packages/graph-fidelity-context/` (src/ first-sight + classify + markers + actions + ledger + telemetry + adapters, test/)|`openspec/specs/graph-fidelity-context/spec.md`|graph-fidelity||core|
|platform-hooks-sdk|Engine-feature domain — platform hooks unified SDK (ADR 0192, from graph-fidelity.md rev.11 strip): unified HookSet contract (sync + async dual-face API, unified handler signature), adapter translation tables (OMP return-style / opencode v1 mutation-style / opencode v2 reserved slots), bind(adapter, handlers) registry, standalone install/test/publish (public npm, private:false), never installed into a platform; graph-fidelity + graph-fidelity-context consume the SDK (platform-binding layer deleted)|`packages/platform-hooks-sdk/` (src/ core + adapters, test/)|`openspec/specs/platform-hooks-sdk/spec.md`|graph-fidelity, graph-fidelity-context||core|
|prompt-assembly-probe|Probe test suite instrumenting the OMP prompt-assembly process and platform seams (npm `@oh-my-pi/pi-coding-agent@17.2.12` exact; zero reference-tree usage; E-group OMP face = native tier (context seam / supersedeReads / dropUseless); F-group position coordinates per seam map (no S4 — tool surface seam-governed); G-group live seam assertions + installed-plugin (`omp plugin list`) + distribution-manifest assertions; coordinates per signal-distribution seam map)|`packages/prompt-assembly-probe/` (package.json, test/ six groups, README.md, outputs/ runtime artifacts)|`openspec/specs/prompt-assembly-probe/spec.md`|signal-distribution||supporting|
|signal-distribution|Signal distribution — single standard for how signals enter, land, and live in the model context across platform seams (ADR 0146, seam-map wording per ADR 0150): seam map (class C1–C4 × seam × fidelity L0–L3) with the seam law (one emission seam per class), the platform native tier (adopted, never re-implemented), and the seam-mapping clause; R1–R5 needs-basis (reference, never a coordinate axis)|`openspec/specs/signal-distribution/spec.md`|`openspec/specs/signal-distribution/spec.md`|—||core|
|token-lifecycle|Token lifecycle — the three-face token governance model (ADR 0155): birth (prompt face — what content in what style enters) / life-death (context face — selection, position, fidelity L0–L3, prune, archive) / measurement (feedback face — usage facts, audit, nudge, reward), closed by the feedback loop; P0–P3 prompt classification (system/session/node-resident + cold-read, ADR 0158) with resident prompts on the S3 system-prompt seam|Shared assets — behavior spec of the token-lifecycle feature point; implementation in CONTEXT.md glossary (three-face model) + atom-kernel (P0–P3 classification) + graph-fidelity (mechanical tier faces)|`openspec/specs/token-lifecycle/spec.md`|signal-distribution||core|
|display-minimalism|Display minimalism — the three display laws (ADR 0156): single-line echo, single-source metering, behavior-reward pairs; display tiering (mechanical seam-live / prose degrade baseline) with the two delivery surfaces (context seam echo + degrade-baseline prose tier)|Shared assets — behavior spec of the display-minimalism feature point; implementation in graph-fidelity (echo rendering — R1 surface; R2 settlement/metering ACTIVE via the context module, ADR 0190/0191 — benefit ledger + gated settlement line + echo value-ratio segment) + atom-phase-handler/atom-pilot (tiering consumption) + CONTEXT.md glossary (three laws)|`openspec/specs/display-minimalism/spec.md`|signal-distribution||core|
|questioning-primitives|Questioning primitives — scenario-separated approval/interview/grilling contracts, explicit interview() participation, grilling graph-dispatch encapsulation; implementation in atom-kernel (§approval()/§interview() contracts) + CONTEXT.md glossary|Shared assets — behavior spec of the questioning-primitives feature point; implementation in atom-kernel (§approval/§interview contracts) + CONTEXT.md glossary|`openspec/specs/questioning-primitives/spec.md`|atom-kernel||core|
|task-templates|Builtin task-template directory (ADR 0232, revised by 0233) — compile-time content assets: handoff template as a standalone TS module in `src/task-templates/` with an index registration surface (named exports + `TASK_TEMPLATES` list = {handoff}); handoff template carries the session contract (two-element result {tasks_done, outputs} returned to the session — no report path, no file write; delegation template deleted with cross-run, ADR 0233); new builtin template = new file + index export + list entry — zero compiler edits|`packages/graph-scheduler/src/task-templates/` (handoff.ts, index.ts)|`openspec/specs/task-templates/spec.md`|graph-definition||core|
|execution-output|Three-tier output model — execution output lives in conversation/session or durable artifacts (user-declared paths), never scheduler run state and never `.taskflow/` writes; feedback channels map to primitives (decision — approval() cards + Decision Request; status — node-boundary lines + final report; risk — inline + structured markers); sub-agent results return as compact structured receipts (status + contract fields + artifact refs); run state is scheduler-owned progress only|Shared assets — behavior spec of the execution-output feature point; implementation in atom-kernel (§task() output contract + receipt contract), atom-pilot (DISPLAY.md), run-record (progress-only scheduler state), phase-handler (session persistence)|`openspec/specs/execution-output/spec.md`|atom-kernel, atom-pilot, run-record, phase-handler||core|

## Asset Reverse Mapping (Asset → Domain)

|Asset|Domain|
|-|-|
|`packages/graph-workflow/skills/<name>/SKILL.md` (17)|Same-named skill domain|
|`packages/graph-scheduler/graphs/<name>.yaml` (12) + `registry.json`|Same-named graph domain; registry.json additionally noted under graph-registry|
|`packages/graph-scheduler/src/fsm/` (4 files), `packages/graph-scheduler/src/topology.ts`, `packages/graph-scheduler/src/flow-flatten.ts`, `packages/graph-scheduler/src/graph-definition.ts`, `packages/graph-scheduler/src/types.ts`, `packages/graph-scheduler/src/schemas/` (7 files), `packages/graph-scheduler/src/context/` (2 files), `packages/graph-scheduler/src/phase-handler/` (6 files), `packages/graph-scheduler/src/lib/db/` (4 files), `packages/graph-scheduler/src/api/` (7 files), `packages/graph-scheduler/src/registry-loader.ts`, `packages/graph-scheduler/src/scheduler-runtime.ts`, `packages/graph-scheduler/src/runtime-start.ts`, `packages/graph-scheduler/src/debug.ts`, `packages/graph-scheduler/src/filesystem.ts`, `packages/graph-scheduler/src/hlt-classes.ts` (high-level-tool), `packages/graph-scheduler/src/config-service.ts` (channels-context-model)|Corresponding engine-feature domain (see detail tables)|
|`packages/graph-scheduler/src/task-templates/` (handoff.ts, index.ts) + `packages/graph-scheduler/tests/unit/task-templates.test.ts`|task-templates|
|`packages/graph-scheduler/src/flow.ts` (mermaid-subset grammar parse) + `packages/graph-scheduler/src/compile.ts` (flow transition-table compile + jump ancestor closure) + `openspec/specs/graph-flow/spec.md`|graph-flow|
|`openspec/specs/graph-flow-layout/spec.md`|graph-flow-layout|
|`packages/graph-scheduler/graphs/{fp-loop-body,arch-loop-body,apply-review-body,engineer-review-body,generate-review-body,maintain-review-body,release-prep-body}.yaml` (deleted with loop desubgraphification, ADR 0240 — loops are parent flow self-edges; all seven loop-body yamls deleted)|fp-loop-body / arch-loop-body / apply-review-body / engineer-review-body / generate-review-body / maintain-review-body / release-prep-body (retired — rows kept for trace)|
|`docs/domains.md`|governance asset — no domain row (index format governed by atom-domain-spec, maintenance by atom-doc-maintain)|
|`README.md` (root), `CHANGELOG.md` (root), `docs/README.zh-CN.md`, `docs/CHANGELOG.zh-CN.md`, `docs/readme-blueprint.md`, `CONTEXT.md`, `docs/workflow.md`, `docs/philosophy.md`, `docs/design.md`, `docs/conventions.md`, `docs/requirements.md`, `docs/constraints.md`, `docs/core-requirements.md`, `docs/specs/` (40 files), `docs/platform/` (README.md + 10 subdirectories), `docs/designs/`, `docs/grill/`, `docs/tickets/`, `docs/agents/` (3 files)|governance assets — no domain row|
|`docs/adr/`|governance assets — no domain row (lifecycle per atom-doc-lifecycle / atom-adr-maintain)|
|`standards/`|governance assets — no domain row (rules carried by CODING-STANDARDS files + constraints channel)|
|`docs/reports/`|arch-review / arch-review-loop (artifacts)|
|`docs/adopt/`|adopt-with-docs (artifacts)|
|`openspec/changes/graph-perception-list/specs/**` (5 delta specs — pending archive sync; spec dir lands at `openspec/specs/graph-perception-list/`)|graph-perception-list|
|`packages/graph-workflow/skills/atom-kernel/SKILL.md` (§Tool Discipline + tool schema tables), `packages/graph-workflow/skills/atom-kernel/SERENA-SCHEMAS.md`, `packages/graph-workflow/skills/atom-kernel/JCODEMUNCH-SCHEMAS.md` (single-home parameter tables), `packages/graph-workflow/skills/atom-phase-handler/SKILL.md` (evidence-only Tool usage check), `packages/graph-workflow/skills/atom-pilot/SKILL.md...|hlt-heat-layering (retired) — shared assets; HLT-REGISTRY.md deleted with the registry (ADR 0194)|
|`packages/graph-fidelity/` (src/ + adapters, test/), `.claude-plugin/marketplace.json` graph-fidelity entry (generated), `opencode.json` (plugin registration)|graph-fidelity|
|`packages/graph-fidelity/src/hints.ts` (5 static hint blocks + HintDisplayFn display fn + tool-name arrays — former texts.ts + PROMOTED_TOOL_MAP, hints-structure-simplify), `packages/graph-fidelity/src/resident-data.ts` (PCL + derived five-scenario enumeration + jcodemunch; former `src/core/` flattened; classify.ts never in HEAD, index.ts facade + pre-execution.ts + session.ts deleted), `packages/platform-hooks-sdk/src/core/` (scenarios.ts, classify.ts)|scenario-tool-hints|
|`packages/graph-fidelity-context/` (src/ first-sight + classify + markers + actions + ledger + telemetry + adapters, test/)|graph-fidelity-context|
|`packages/platform-hooks-sdk/` (src/ core + adapters, test/)|platform-hooks-sdk|
|— (deleted; plugin prototype tree removed with retirement, ADR 0146)|opencode-hlt-policy (retired)|
|— (deleted; assets absorbed into graph-fidelity)|context-fidelity (retired)|
|`packages/prompt-assembly-probe/` (package.json, test/ six groups, README.md, outputs/ runtime artifacts)|prompt-assembly-probe|
|`packages/graph-workflow/skills/atom-kernel/SKILL.md` (§task() output contract + receipt contract), `packages/graph-workflow/skills/atom-pilot/DISPLAY.md`, `packages/graph-scheduler/src/lib/db/` (progress-only run state)|execution-output (shared assets)|
|`packages/graph-workflow/skills/atom-phase-handler/SKILL.md` (§Checks block `context:` row — estimate-only), `packages/graph-workflow/skills/atom-kernel/SKILL.md` (§Tool Schemas — serena/jcodemunch/graph-scheduler tables), `packages/graph-workflow/skills/atom-pilot/DISPLAY.md` (§Context stats — Checks-rows-only)|context-usage-contract (retired — reference-suspended, ADR 0175)|
|Root `skills/` (9 SKILL.md, retired; 17 historical directories — removed from disk 2026-08-04)|Takes no row — noted as a whole as retired|

**Completeness rule**: every asset maps to exactly one domain (or explicitly noted as shared/artifact); every domain has at least one asset; any asset without a domain = documentation defect. Domain IDs strictly match asset names; asset paths written in full path form so validation is mechanical.

## Evolution Rules

Add/modify/delete a domain follows a four-step process:

1. **Intent** — why the domain is needed (new asset? unclear boundary?)
2. **Boundary** — boundary against adjacent domains; kind classification
3. **Asset registration** — all assets into the mapping table, updated in both directions
4. **Naming de-duplication** — domain ID strictly matches asset name, globally unique

Companion: this file forbids building a `docs/domains/` directory tree; a new asset not registered = evolution violation.

## Linkages

- glossary "domain index" entry (CONTEXT.md) — defines this file's responsibility and boundary.
- CONTEXT.md Docs map — references this file.
